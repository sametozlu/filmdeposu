services:
  - type: web
    name: filmdeposu
    runtime: docker
    plan: free
    healthCheckPath: /health
    envVars:
      - key: Jwt__Key
        generateValue: true
      - key: Admin__Password
        generateValue: true
      # Opsiyonel: TMDB/OMDb anahtarları (Render dashboard'dan da eklenebilir)
      # - key: TMDB_API_KEY
      #   sync: false
      # - key: OMDB_API_KEY
      #   sync: false
      # Opsiyonel: kalıcı PostgreSQL için DATABASE_URL ekleyin
      # - key: DATABASE_URL
      #   sync: false
